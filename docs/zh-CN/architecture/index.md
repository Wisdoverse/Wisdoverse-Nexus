# 架构概述

Nexis 是一个模块化的 AI 原生协作平台，采用 Rust 异步架构。

## 核心设计原则

1. **高性能** — 基于 Tokio 异步运行时，支持 10 万+ 并发连接
2. **模块化** — 每个功能独立 crate，可单独使用
3. **可扩展** — 插件系统支持 Wasm 沙箱隔离
4. **云原生** — 12-Factor App 完全合规

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层                          │
│   Web Client  │  Mobile App  │  CLI  │  Third-party         │
└──────────────────────┬─────────────────────────────────┘
                     │ WebSocket / HTTP
┌──────────────────────▼─────────────────────────────────┐
│                   nexis-gateway                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ Auth     │ │ Router   │ │ Rate     │ │ Metrics  │        │
│  │ Middleware│ │          │ │ Limiter  │ │          │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
└──────────────────────┬─────────────────────────────────┘
                     │
┌──────────────────────▼─────────────────────────────────┐
│                     核心层                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ nexis-   │ │ nexis-   │ │ nexis-   │ │ nexis-   │        │
│  │ core     │ │ protocol │ │ context  │ │ memory   │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
└──────────────────────┬─────────────────────────────────┘
                     │
┌──────────────────────▼─────────────────────────────────┐
│                      扩展层                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ nexis-   │ │ nexis-   │ │ nexis-   │ │ nexis-   │        │
│  │ plugin   │ │ mcp      │ │ ai       │ │ skills   │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
└─────────────────────────────────────────────────────────────┘
```

## 核心 Crates

| Crate | 职责 |
|-------|------|
| `nexis-core` | 基础类型、Traits、错误处理 |
| `nexis-gateway` | WebSocket 网关、HTTP API |
| `nexis-protocol` | 消息协议定义 |
| `nexis-context` | 会话上下文管理 |
| `nexis-plugin` | Wasm 插件系统 |
| `nexis-memory` | 长期记忆存储 |

## 技术栈

- **运行时**: Tokio 1.x
- **HTTP/WebSocket**: Axum + axum-extra
- **序列化**: Serde
- **认证**: JWT (jsonwebtoken)
- **追踪**: OpenTelemetry + Tracing
- **测试**: 内置 test framework + tokio-test

## 数据流

参见 [数据流](/zh-CN/architecture/data-flow) 了解消息如何在系统中流转。

## 下一步

- [数据流](/zh-CN/architecture/data-flow) — 消息流转机制
- [组件](/zh-CN/architecture/components) — 各组件详细说明
- [性能报告](/zh-CN/performance/benchmark-report) — 性能基准测试
