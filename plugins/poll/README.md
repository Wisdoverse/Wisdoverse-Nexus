# Poll Plugin for Nexis

Create and manage polls in chat rooms.

## Features

- `/poll create "Question" "Option1" "Option2" ...` — create a poll
- `/poll vote <id> <option>` — vote on a poll
- `/poll results <id>` — view poll results
- `/poll close <id>` — close a poll
