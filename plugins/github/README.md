# GitHub Plugin for Nexis

Webhook integration for GitHub PR/Issue events.

## Features

- Receive PR and Issue webhook events
- Post formatted notifications to rooms
- `/github issue list` — list open issues
- `/github issue create <title>` — create a new issue

## Configuration

Set `GITHUB_TOKEN` environment variable for API access.
