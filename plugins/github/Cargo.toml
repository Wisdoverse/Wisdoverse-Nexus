[package]
name = "nexis-plugin-github"
version = "0.1.0"
edition = "2021"
license-file = "../../LICENSE"

[dependencies]
nexis-plugin = { path = "../../crates/nexis-plugin" }
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
