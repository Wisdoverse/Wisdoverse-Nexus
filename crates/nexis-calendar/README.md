# nexis-calendar

Calendar and scheduling domain crate for events, conflict detection metadata, and reminders.

## Modules

- `event`: `CalendarEvent`, `EventAttendee`, `ResponseStatus`
- `conflict`: `Conflict`, `ConflictSeverity`
- `reminder`: `Reminder`, `ReminderRule`
- `error`: `CalendarError`, `CalendarResult`
