apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nexis-gateway.fullname" . }}
  labels:
    {{- include "nexis-gateway.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
